<UserControl x:Class="Caliburn.Micro.Hello.ConductorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Caliburn.Micro.Hello"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.25*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <Button Name="ShowPageOne"  Content="Show Page One" HorizontalAlignment="Left" MinWidth="200" Margin="50,20,50,20"/>
            <Button Name="ShowPageTwo"  Content="Show Page Two" HorizontalAlignment="Right" MinWidth="200" Margin="50,20,50,20"/>
        </DockPanel>
        <ContentControl Name="ActiveItem" Grid.Row="1"/>
    </Grid>
</UserControl>
